fabric.utile.array 命名空间

类函数成员：
static Array invoke(Array, String)：调用附于指定数组的每个成员上的方法函数。参数一为指定的数组，参数二为函数名（译者注：从源码可知，参数二为数组中每个成员上都有一的一个属性名，该属性是一个函数，调用该函数的参数为invoke前两个参数之外的后面所有参数。作者这种思路实在太魔性，太飘逸了，OMZ）。
static Any max(Array, String)：寻找数组中的最大值元素。参数一为指定的数组；参数二为属性名，如果数组中的元素为Object，可以用该属性名对应的值进行比较。
static Any min(Array, String)：寻找数组中的最小值元素。参数一为指定的数组；参数二为属性名，如果数组中的元素为Object，可以用该属性名对应的值进行比较。





